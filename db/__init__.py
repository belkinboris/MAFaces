"""Слой БД — фундамент под алерты, верификацию фирм и тарифы (см. обсуждение
в вики/переписке про lock-in). Пока не подключён к main.py: живёт отдельно,
ничего в проде не меняет, пока его туда осознанно не вкрутят.

DATABASE_URL по умолчанию — локальный SQLite-файл; на Timeweb достаточно
задать переменную окружения DATABASE_URL с адресом Postgres, код не меняется.
"""
